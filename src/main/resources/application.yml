spring:
  jpa:
    show-sql: true
    database: postgresql
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/fiaprestaurant-db}
    username: ${SPRING_DATASOURCE_USERNAME:fiaprestaurant-postgres-user}
    password: ${SPRING_DATASOURCE_PASSWORD:fiaprestaurant-postgres-pwd}
  flyway:
    baseline-on-migrate: true
    schemas: 'flyway'
  docker:
    compose:
      enabled: true
      file: docker-compose.yml
  mail:
    host: smtp.ethereal.email
    port: 587
    username: kayla45@ethereal.email
    password: x34sZvrdC1D8eZmpbq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

server:
  port: ${SERVER_PORT:8080}
  error:
    include-stacktrace: never

api:
  security:
    token:
      secret: ${API_SECURITY_TOKEN_SECRET:Fi@p-@lur@-2ADJT}
